/** Journal colophon footer. */
export default function Footer() {
  return (
    <footer className="fm-footer">
      <div className="fm-footer-inner">
        <span className="fm-footer-name">FreakyMustard</span>
        <span>Set in Fraunces &amp; Inter. Printed on pixels, bound nowhere.</span>
        <span>Lists live in your browser only — no accounts, no tracking.</span>
      </div>
    </footer>
  );
}
